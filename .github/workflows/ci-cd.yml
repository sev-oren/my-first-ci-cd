name: Simple Passing Pipeline

on: [push, pull_request]

jobs:
  always-pass:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Always Succeed
        run: |
          echo "✅ Pipeline completed successfully!"
          echo "✅ This will always pass!"
